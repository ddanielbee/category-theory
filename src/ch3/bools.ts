export const same = (a: boolean): boolean => a;
export const opposite = (a: boolean): boolean => !a;
export const always = (_: boolean): boolean => true;
export const never = (_: boolean): boolean => false;
